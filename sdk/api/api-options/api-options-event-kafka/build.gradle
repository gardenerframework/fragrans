dependencies {
    api group: "${rootProject.group}", name: 'api-options-common'
    api "org.springframework.kafka:spring-kafka"
    testImplementation "org.springframework.kafka:spring-kafka-test"
}